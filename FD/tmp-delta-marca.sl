       SELECT tmp-delta-marca
           ASSIGN       TO  path-tmp-delta-marca
           ORGANIZATION IS INDEXED
           ACCESS MODE  IS DYNAMIC
           FILE STATUS  IS STATUS-tmp-delta-marca
           RECORD KEY   IS tmdm-chiave.
